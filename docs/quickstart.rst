Quickstart
==========

1. Create and activate the dedicated environment:

   .. code-block:: bash

      python3 -m venv pysse_env
      source pysse_env/bin/activate

2. Install dependencies:

   .. code-block:: bash

      pip install --upgrade pip
      pip install -r requirements.txt

3. Run one of the notebooks in ``notebooks/`` to validate the workflow.
